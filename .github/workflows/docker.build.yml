name: Build and Push Docker Image

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code # Copia el código del repositorio
      uses: actions/checkout@v4

    - name: Set up Docker Buildx 
      uses: docker/setup-buildx-action@v3

    - name: Cache Docker layers 
      uses: actions/cache@v4
      with:
        path: /tmp/.buildx-cache
        key: ${{ runner.os }}-buildx-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-buildx-

    - name: Build Docker image 
      run: docker build . -t noteburner # Cambia el nombre

    - name: Push Docker image 
      run: docker tag noteburner ghcr.io/${{ github.repository }}/noteburner:latest # Cambia los nombres
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Publish Docker image
      run: docker push ghcr.io/${{ github.repository }}/noteburner:latest # Cambia el nombre
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
